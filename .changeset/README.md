# Changesets

This directory is intentionally empty (no pending `*.md` changesets) so the
release orchestrator's "Skip if pending changesets" gate evaluates to
`skip=false` and proceeds — mirroring upstream behavior.
